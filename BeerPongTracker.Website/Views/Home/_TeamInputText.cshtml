@using BeerPongTracker.Website.Models
@model BeerPongTracker.Website.Models.TeamInputTextViewModel

<div class="team-input__col team-input__col--1 team-input__col--text">
    <div class="team-input__col__inner">
        @Html.Partial("_TeamInputTextBox", new TeamInputTextBoxViewModel() {TeamId = Model.TeamId , PlayerId = 1})
        @Html.Partial("_TeamInputTextBox", new TeamInputTextBoxViewModel() { TeamId = Model.TeamId, PlayerId = 2 })
        @Html.Partial("_TeamInputTextBox", new TeamInputTextBoxViewModel() { TeamId = Model.TeamId, PlayerId = 3 })
        @Html.Partial("_TeamInputTextBox", new TeamInputTextBoxViewModel() { TeamId = Model.TeamId, PlayerId = 4 })
    </div>
</div>